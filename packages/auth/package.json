{
  "name": "@jjangga0214/auth",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "_moduleAliases": {
    "~auth": "dist"
  },
  "scripts": {
    "_": "dotenv -e ../../.env --",
    "build": "tsc --build tsconfig.build.json && link-module-alias",
    "clean": "tsc --build tsconfig.build.json --clean && shx rm -rf dist coverage *.log junit.xml && jest --clearCache",
    "test": "jest",
    "test:unit": "jest test/unit",
    "test:watch": "jest --watch",
    "test:ci": "jest --coverage --ci --reporters='jest-junit'",
    "coverage": "jest --coverage",
    "coverage:show": "live-server coverage/lcov-report",
    "ts": "yarn _ ts-node-dev --respawn -r tsconfig-paths/register",
    "dev:debug": "yarn ts src/index.ts",
    "dev": "pm2 start --restart-delay 250 --name auth \"yarn dev_\"",
    "down": "pm2 delete auth",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@jjangga0214/request": "^1.0.0",
    "@apollo/federation": "^0.11.3",
    "apollo-server": "^2.9.16",
    "bcryptjs": "^2.4.3",
    "graphql": "^14.5.8",
    "graphql-import": "^1.0.0-beta.1",
    "jsonwebtoken": "^8.5.1"
  },
  "devDependencies": {}
}
